/***
 * Math对象
 * 内核直接提供的静态对象
 * 
 * Math.abs() 绝对值
 * Math.sin() cos() tan()
 * Math.max() 返回参数中最大的值
 * Math.min() 返回参数中最小的值
 * Math.random() 返回一个随机数 伪随机数 0-1之间
 */
console.log(Math.max(1,2,3,4,5));
console.log(Math.min(1,2,3,4,5));
var num=Math.random();
console.log(num);
